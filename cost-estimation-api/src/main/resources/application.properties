spring.datasource.url=jdbc:postgresql://localhost:5431/costestimation
spring.datasource.username=costestimation
spring.datasource.password=costestimation

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect

spring.jpa.show-sql=true
spring.jpa.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

#comando docker para criar um container a partir de uma imagem docker do postgres
# sudo docker run -p 5431:5432 --name costestimation -e POSTGRES_USER=costestimation -e POSTGRES_PASSWORD=costestimation -e POSTGRES_DB=costestimation -d postgres:10.5-alpine


server.port=8085

# user cost estimation

# utilizado no recurso AWS API DevOps Guru e AWS API CloudFormation
amazon.cost.estimation.accesskey=
amazon.cost.estimation.secretkey=
