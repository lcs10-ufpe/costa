spring.datasource.url=jdbc:postgresql://localhost:5434/executor
spring.datasource.username=executor
spring.datasource.password=executor

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect

spring.jpa.show-sql=true
spring.jpa.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

#comando docker para criar um container a partir de uma imagem docker do postgres
# sudo docker run -p 5434:5432 --name executordb -e POSTGRES_USER=executor -e POSTGRES_PASSWORD=executor -e POSTGRES_DB=executor -d postgres:10.5-alpine


# API budgeting-api
base.url.budgeting.api=http://localhost:8082

jenkins.user=lcs10
jenkins.token=11444e6eb7ea506eca393bb4c4d08d8993

server.port=8084
