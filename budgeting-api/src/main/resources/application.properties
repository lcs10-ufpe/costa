spring.datasource.url=jdbc:postgresql://localhost:5432/budgeting
spring.datasource.username=budgeting
spring.datasource.password=budgeting

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect

spring.jpa.show-sql=true
spring.jpa.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

#comando docker para criar um container a partir de uma imagem docker do postgres
# sudo docker run -p 5432:5432 --name budgetingdb -e POSTGRES_USER=budgeting -e POSTGRES_PASSWORD=budgeting -e POSTGRES_DB=budgeting -d postgres:10.5-alpine


server.port=8082